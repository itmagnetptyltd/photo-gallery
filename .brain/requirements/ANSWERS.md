# Answers

Paste the client's reply here, under the question it answers. Their words, not a
summary — the wording behind every acceptance criterion stays in this file.

Then run `/resolve-ambiguities`. That writes the acceptance criteria, clears the
answered questions from both this file and `AMBIGUITIES.md`, and moves whatever
is now unambiguous from `draft` to `agreed`.

You do not have to answer all 28 at once. Answering any question unblocks the
requirements that carry it; the rest stay `draft`.

---

## 1. Does "gallery" mean the whole application, or one named collection of Photos inside it?

Gallery means the whole application and its single collection of Photos. There are no named collections or multiple galleries.

## 2. Is there any concept of an Owner or of signing in at all, or is every Photo visible to anyone who can reach the application?

There is no Owner concept and no sign-in. The application is local and every Photo is visible to anyone who can reach the application.

## 3. Is the application only ever used by one person on one PC, or must it serve several people at the same time?

The application is intended for use on one local development PC by one person. Multi-user concurrent access is out of scope.

## 4. Does "photo" cover still images only, and which file formats must be accepted?

Photo means still images only. Accept JPEG and PNG files.

## 5. What is the largest file a person may upload, and what must they see when they exceed it?

The maximum upload size is 10 MB per Photo. If the file exceeds the limit, reject it and show a clear message in the upload modal.

## 6. What must happen when a person chooses a file that is not an accepted image, and must that be detected before the file is transferred?

Only accepted image files are allowed. Validate the selected file in the browser and validate it again on the server using the file content/type. Reject invalid files with a clear error message.

## 7. May more than one Photo be uploaded in a single use of the modal?

Only one Photo may be selected and uploaded per use of the modal.

## 8. May an Upload in progress be cancelled, and what must happen if the modal is closed before the Upload completes?

An Upload cannot be cancelled once it has started. Closing the modal while an Upload is in progress is disabled until the Upload completes or fails.

## 9. What must the person see when an Upload fails part-way, and must a failed Upload leave nothing behind?

If an Upload fails, show a clear error message and allow the person to retry. A failed Upload must leave no Photo record and no partial file behind.

## 10. Are two Uploads of the same image file two Photos or one?

Every Upload creates a new Photo. Uploading the same image file twice creates two Photos.

## 11. Must the Original be retained unchanged after Upload, and must anyone be able to retrieve it?

The Original is retained unchanged after Upload. The application does not provide a separate download feature; the Original is used when the Photo is opened in the larger view.

## 12. Is the thumbnail a separate smaller Rendition produced by the system, or the Original delivered whole and shown at a reduced size?

The thumbnail is a separate smaller Rendition generated by the application. The grid uses the thumbnail rather than loading the full Original.

## 13. What information about a Photo, if any, must be shown beside it in the grid or in the larger view?

No additional Photo information is required in the grid or larger view. Show the image only.

## 14. In what order must Photos appear in the grid, and when the client says "date" do they mean Capture date or Upload date?

Photos appear newest Upload first. The ordering is based on Upload date/time, not Capture date.

## 15. What must happen when there are more Photos than fit on one screen — does "all uploaded photos" still mean all of them at 10,000?

Photos are displayed using pagination or incremental loading rather than requiring thousands of Photos to be rendered at once. The application must support the available local storage without a fixed small gallery-size limit.

## 16. What must the home page show when no Photo has been uploaded yet?

When no Photo has been uploaded, show an empty-state message inviting the person to upload a Photo, together with the Upload Photo control.

## 17. Must an existing Photo ever be removed or renamed by anyone, and if it is removed, is it recoverable?

Existing Photos may be removed. The delete action is available from the Photo tile/grid, requires confirmation, and permanently removes the Photo and its stored files. Renaming is not supported.

## 18. Does "immediately displayed" apply only to the person who uploaded the Photo, or must an already-open grid belonging to someone else update too?

Immediately displayed means the Photo uploaded by the person is added to that person's currently open grid without requiring a full page reload. Live updates to other already-open grids are out of scope.

## 19. What does "a larger view" mean — an overlay on the home page or a separate page — and at what resolution is the Photo shown?

The larger view is an overlay/modal on the home page. It displays the Photo at a size that fits the available viewport while allowing the person to see the image properly; it does not navigate to a separate Photo page.

## 20. Must the larger view offer navigation to the next and previous Photo, and how must it be dismissed?

The larger view supports previous and next Photo navigation. It is dismissed with a close control, and the Escape key may also close it.

## 21. Which devices, browsers and viewport widths must the interface support?

The application must support desktop development PCs using current Chrome, Edge, and Firefox browsers. The layout must reflow for common desktop viewport sizes; mobile phones and tablets are out of scope.

## 22. What must be operable without a mouse, and against which accessibility standard, if any, will the interface be judged?

All primary actions must be keyboard operable, including tabbing to controls, opening a Photo with Enter, and closing the larger view with Escape. A formal WCAG conformance level is not required.

## 23. Who decides whether the interface is "clean", "attractive" and "user-friendly", and what evidence would make it acceptable or unacceptable?

The delivery team will use a simple modern visual design as the acceptance baseline: consistent spacing, clear controls, readable text, responsive grid reflow, and no unnecessary visual clutter. The client can accept or request changes during the demo.

## 24. Must uploaded Photos still be in the Gallery after the application is stopped and started again?

Uploaded Photos must persist after the application and PC are stopped and started again. Photos are stored on disk and their metadata is stored in SQLite.

## 25. Is there a limit on total storage, and what must happen when it is reached?

There is no application-level total storage quota. Uploads continue until the available local disk space is exhausted; a disk-full condition must fail cleanly without leaving a partial Photo.

## 26. What counts as an "unnecessary" dependency, and who approves the dependency list?

Avoid unnecessary dependencies means do not add libraries unless they provide functionality that is not reasonably available from Node.js or the application code. A small number of established dependencies needed for the web server, SQLite access, and image thumbnail generation are acceptable.

## 27. Which Node.js versions and which operating systems must the application run on?

Use a current Node.js LTS release on Windows for the local development PC. Other operating systems are out of scope.

## 28. What does "easy to run on a local development PC" mean in checkable terms — how many steps, and what may a person be expected to install first?

Easy to run means Node.js must be installed first, then the project can be started with the documented install and run commands. No database server, cloud service, Docker, or other external infrastructure is required.


---


# Answers — second brief, Sep-01-2026


# Answers — second brief, Sep-01-2026

Questions 29-50 arise from KCB's feedback on the first delivery, captured as
`FB-0001` and scoped by `CHG-0001`. All 22 were answered on Sep-01-2026.

Run `/resolve-ambiguities` to write these into the acceptance criteria of
REQ-PHOTO-016 to REQ-PHOTO-024, clear questions 29-50 from `AMBIGUITIES.md` and
the `ambiguities` lists in `photo.yaml`, and move those requirements from `draft`
to `agreed`.

The 28 answers above are the record of the first brief and stay where they are.

## 29. Who judges "professional", and against what?

There is no reference site or screenshot to be judged against. "Professional"
means built to an agreed design token sheet: one page setting out the palette,
typeface, text sizes, spacing steps, corner radius, focus ring and button
variants, approved before any styling is built. Acceptance is a single demo
against that approved sheet. Anything requested after the sheet is approved is a
variation, not a defect.

## 30. Who supplies the design values — you or us?

The delivery team proposes the values and the client approves them in one round
before the work starts. The token sheet covers the palette, typeface stack,
text-size scale, spacing scale, corner radius, focus ring and button variants.
REQ-PHOTO-016 to REQ-PHOTO-024 are estimable once the sheet is approved.

## 31. Is there a palette, and is it light or dark?

There are no brand colours that must be used. Use a neutral light palette: a
near-white page, white surfaces for the app bar, tiles and modals, a dark neutral
for text, a mid neutral for borders and quiet controls, and a single accent
colour used only for the primary action. A dark appearance is out of scope for
this change.

## 32. Which typeface, and may it be fetched over the network?

Use the typefaces already present on the person's computer, specified as a system
font stack. No font file is shipped with the application and no font is
downloaded when the page opens, so REQ-PHOTO-014@v1 and REQ-PHOTO-015@v1 are
unaffected.

## 33. What is "the product name"?

The app bar reads **Photo Gallery** — the words already on the page. The Gallery
entry in `.brain/glossary.md` is amended to record that the Gallery has no
identifier but does have a display name, "Photo Gallery", shown in the app bar.

## 34. Where does the app bar sit, and what does it do when the page scrolls?

The app bar sits at the top of the page and scrolls away with the content. It is
not fixed, so no strip of the viewport is permanently removed from the Grid and
REQ-PHOTO-011@v1 is unaffected.

## 35. Does the "+ Upload Photo" control move into the app bar?

The app bar carries the product name only. The "+ Upload Photo" control stays
where it is today, above the Grid, as REQ-PHOTO-003@v1 and REQ-PHOTO-011@v1
criterion 2 describe.

## 36. Is the app bar in front of the Larger view, or behind it?

When a Photo is open in the Larger view the overlay covers the app bar, and the
Photo has the whole viewport. This keeps the space REQ-PHOTO-009@v1 requires for
the Original and keeps the app bar clear of the close control.

## 37. What changes on hover, and what do touch and keyboard users get instead?

Hover changes appearance only. Buttons shift background and border; tiles lift
slightly and gain a border tint. Nothing is revealed on hover that is not
otherwise shown, so people using a touch screen or the keyboard alone lose
nothing and no second design is needed. Keyboard focus is given the same visual
weight as hover.

## 38. May a tile's hover state show text?

No. A tile shows the image alone whether hovered or not — no filename, date or
caption appears over it. This holds REQ-PHOTO-002@v1 criterion 5 and is
consistent with answer 13 above.

## 39. Which elements have rounded corners besides the tiles?

A single corner-radius value, named on the token sheet, applies to the tiles, the
buttons, the upload modal surface and the Larger view's control surfaces. The app
bar spans the full width of the window and has no visible corners to round.
Because the radius is one shared value, the amount of rounding can be changed
later in one place.

## 40. Is "lightbox" the Larger view, and what is its "chrome"?

"Lightbox" means the Larger view and is recorded in `.brain/glossary.md` as a
client synonym for it. "Chrome" means its close, previous and next controls. The
darkened backdrop behind the Photo stays as delivered and is not restyled under
this change.

## 41. Must the Larger view's controls stay legible over any Photo?

Yes. The close, previous and next controls each sit on their own opaque surface,
and each icon has a contrast ratio of at least 4.5:1 against that surface, so they
remain legible whatever Photo is open, including a very light one. This is a
measurement applied to those three controls and is not a WCAG conformance claim;
answer 22 above still stands.

## 42. Is the upload modal part of this restyle?

Yes. The upload modal is restyled with everything else, using the same type
scale, spacing scale, button variants, surface treatment and corner radius, so
that no view is left looking unfinished at the demo.

## 43. The focus ring already ships — what more do you want?

The ask is already met by the `:focus-visible` outline that ships today. It is
restyled to the token sheet's colour and width so that it matches the new design;
the rule remains and the outline is not removed, so the existing assertion in
`tests/responsive.test.js` continues to hold. No new requirement is written and
REQ-PHOTO-011 is not versioned.

## 44. The empty state already exists — what does "designed" add?

The empty state is restyled with the rest of the application — the same wording,
the same "+ Upload Photo" control, the new type, spacing and button treatment.
Nothing is added to it structurally: no illustration, no heading and no new
words. It stays covered by REQ-PHOTO-001@v1 together with REQ-PHOTO-017,
REQ-PHOTO-018 and REQ-PHOTO-020, and REQ-PHOTO-001 is not versioned.

## 45. Does "professional" extend to small screens?

Desktop widths only, as answer 21 above scoped the first brief. Mobile phones and
tablets remain out of scope, and REQ-PHOTO-011's "common desktop viewport width"
continues to apply.

## 46. Does this include how tiles crop photos, and blurry small photos?

This change is presentation only: rounded corners and a hover state. How much of
a Photo a square tile crops, and how a Photo smaller than its tile is handled, do
not change, so REQ-PHOTO-002@v1 and REQ-PHOTO-006@v1 are untouched. The crop
observation recorded in FB-0001 remains **escalate** and is decided separately
from this brief.

## 47. How is any of this to be checked, given nothing has ever been rendered?

The look is signed off by eye, by the client, at an agreed demo against the
approved token sheet. No browser automation is added and no new dependency is
introduced, so REQ-PHOTO-015@v1 stands. The token values themselves — text
sizes, spacing steps, radius, palette and focus ring — are named in the
stylesheet and asserted by the existing test setup without rendering anything.
It is accepted that on the evidence rule in `requirements/README.md`,
REQ-PHOTO-016 to REQ-PHOTO-024 rest at `agreed` with a recorded demo acceptance
and do not reach `verified`.

## 48. Does "scale" mean a written-down set of steps, or just consistency?

A written-down set of steps that every element is drawn from. The text-size scale
has named steps — app bar title, section heading, body, small, caption — and the
spacing scale is a single base unit with a fixed set of multiples. Both are listed
on the approved token sheet and expressed as named tokens in the stylesheet, so
the list can be read and checked against what is in use.

## 49. May the spacing scale change the gap between tiles?

No. The Grid's existing gap keeps its current value and is adopted as one of the
scale's steps. Spacing between tiles stays uniform and unchanged, so
REQ-PHOTO-011@v1 criterion 3 and the pinned `gap` in `tests/responsive.test.js`
are unaffected.

## 50. One button treatment, or several?

Two variants, and no more. **Primary**, using the accent colour from answer 31,
for "+ Upload Photo" and the confirming action inside the upload modal.
**Secondary**, a quiet neutral surface with a border, for Cancel, the Larger
view's Close, and its Previous and Next. The delete confirmation covered by
REQ-PHOTO-005 uses the secondary treatment; there is no separate destructive
variant.