{
  "name": "photo-gallery",
  "version": "0.1.0",
  "private": true,
  "description": "A simple photo gallery that runs on one local development PC.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/"
  }
}
